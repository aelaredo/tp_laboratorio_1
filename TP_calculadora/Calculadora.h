/** \brief suma dos numero con coma y devuelve un numero con coma
 *
 * \param el primer operando
 * \param el segundo operando
 * \return la suma de ambos operandos
 *
 */
float SumarNumeros (float primerOperando, float SegundoOperando);

/////////////////////////////////////////////////////////////////

/** \brief resta dos numero con coma y devuelve un numero con coma
 *
 * \param el primer operando con coma
 * \param el segundo operando con coma
 * \return la resta de ambos operandos con coma
 *
 */
float RestarNumeros (float primerOperando, float SegundoOperando);

////////////////////////////////////////////////////////////////////

/** \brief divide dos numeros, verificar antes que no se pasen ceros.
 *
 * \param el primer operando con coma
 * \param el segundo operando con coma
 * \return la resta de ambos operandos con coma
 *
 */
float DividirNumeros (float primerOperando, float SegundoOperando);

////////////////////////////////////////////////////////////////////

/** \brief multiplica dos numeros con coma
 *
 * \param el primer operando con coma
 * \param el segundo operando con coma
 * \return el producto de ambos operandos con coma
 *
 */

float MultiplicarNumeros (float primerOperando, float SegundoOperando);

/////////////////////////////////////////////////////////////////////

/** \brief Devuelve el factorial de un numero, del numero con coma, toma solo la parte entera de ese numero
 *
 * \param el numero con coma, de este numero solo se toma la parte entera.
 * \return el factorial de la parte entera del nu
 *
 */

int FactorialNumero (float numero);

